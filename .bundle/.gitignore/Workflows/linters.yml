name: Linters

on: pull_request

jobs:
  rubocop:
    runs-on: ubuntu-22.04

    steps:
      - uses: actions/checkout@v4

      - uses: ruby/setup-ruby@v3
        with:
          ruby-version: 3.4
          bundler-cache: true

      - name: Run Rubocop
        run: bundle exec rubocop --color