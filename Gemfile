source 'https://rubygems.org'

ruby '2.7.0'

# Testing framework
gem 'rspec', '~> 3.10'

# Optional: For better test output
gem 'rspec-core', '~> 3.10'

# Optional: Guard for auto-running tests
gem 'guard', '~> 2.16'
gem 'guard-rspec', '~> 4.7'

# Optional: Code coverage
gem 'simplecov', '~> 0.19.0', require: false

# Development dependencies
group :development do
  gem 'pry'
  gem 'pry-byebug'
end
